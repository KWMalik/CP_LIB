<%= provide(:title, 'Add Book') %>
<h1>Add Book</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@book) do |f| %>
        <%= render 'fields', f: f %>
        <%= f.submit "Add book", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>